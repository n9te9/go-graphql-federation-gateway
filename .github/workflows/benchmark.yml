name: Benchmark Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Manual trigger

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install hey (HTTP load generator)
        run: |
          wget -q https://hey-release.s3.us-east-2.amazonaws.com/hey_linux_amd64
          chmod +x hey_linux_amd64
          sudo mv hey_linux_amd64 /usr/local/bin/hey
          hey -version || echo "✓ hey installed"

      - name: Install Apollo Router
        run: |
          curl -sSL https://router.apollo.dev/download/nix/latest | sh
          sudo mv router /usr/local/bin/
          router --version

      - name: Build gateway binary
        run: |
          go build -o cmd/go-graphql-federation-gateway/gateway cmd/go-graphql-federation-gateway/main.go
          chmod +x cmd/go-graphql-federation-gateway/gateway

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build gateway Docker image
        run: |
          docker build -f _example/Dockerfile.gateway -t federation-gateway:latest .

      - name: Run comprehensive comparative benchmark (All domains)
        id: benchmark-all
        run: |
          cd _example
          make all-compare-benchmark 2>&1 | tee benchmark_results.txt
        continue-on-error: true

      - name: Extract benchmark summary
        if: always()
        id: extract-summary
        run: |
          cd _example
          
          # Extract performance comparison
          if [ -f benchmark_results.txt ]; then
            echo "📝 Extracting benchmark summary..."
            
            # Remove ANSI color codes for clean extraction
            sed 's/\x1b\[[0-9;]*m//g' benchmark_results.txt > benchmark_results_clean.txt
            
            # Debug: Show what we have
            echo "=== Debug: Checking for key markers ==="
            grep -n "Benchmark Results Summary" benchmark_results_clean.txt || echo "⚠️ 'Benchmark Results Summary' not found"
            grep -n "Performance Comparison" benchmark_results_clean.txt || echo "⚠️ 'Performance Comparison' not found"
            echo "========================================"
            
            echo "## 🚀 Benchmark Results Summary" > benchmark_summary.md
            echo "" >> benchmark_summary.md
            
            # Extract the results table
            if grep -q "Benchmark Results Summary" benchmark_results_clean.txt; then
              echo '```' >> benchmark_summary.md
              # Extract from the header to the end of table
              awk '/Benchmark Results Summary/,/Performance Comparison/ { 
                if ($0 !~ /Performance Comparison/) print 
              }' benchmark_results_clean.txt >> benchmark_summary.md
              echo '```' >> benchmark_summary.md
              echo "" >> benchmark_summary.md
            fi
            
            # Extract performance comparison section
            if grep -q "Performance Comparison:" benchmark_results_clean.txt; then
              echo "### 📊 Performance Comparison" >> benchmark_summary.md
              echo '```' >> benchmark_summary.md
              awk '/Performance Comparison:/,/Benchmark completed/ { 
                if ($0 !~ /Benchmark completed/) print 
              }' benchmark_results_clean.txt >> benchmark_summary.md
              echo '```' >> benchmark_summary.md
            fi
            
            # Save for artifact and show what we extracted
            cp benchmark_summary.md ../benchmark_summary.md
            
            echo "✓ Summary extracted"
            echo "=== Extracted Summary Preview ==="
            head -30 benchmark_summary.md
            echo "================================="
          else
            echo "⚠️ Benchmark results file not found"
            ls -la
          fi

      - name: Upload benchmark results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: |
            _example/benchmark_results.txt
            benchmark_summary.md
          retention-days: 90

      - name: Comment PR with benchmark results
        if: github.event_name == 'pull_request' && always()
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            
            let comment = '## 🚀 Benchmark Results (Go Gateway vs Apollo Router)\n\n';
            
            try {
              if (fs.existsSync('benchmark_summary.md')) {
                const summary = fs.readFileSync('benchmark_summary.md', 'utf8');
                
                // Check if summary has meaningful content
                if (summary.trim().length > 100) {
                  comment += summary;
                } else {
                  comment += '⚠️ Benchmark completed but results were too short or empty.\n\n';
                  comment += '<details>\n<summary>Debug Info</summary>\n\n';
                  comment += '```\n' + summary + '\n```\n';
                  comment += '</details>\n\n';
                }
              } else {
                comment += '⚠️ Benchmark completed but summary file was not found.\n\n';
              }
              
              comment += '\n---\n';
              comment += '📊 Full benchmark results are available in [Actions Artifacts](';
              comment += `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`;
              
            } catch (e) {
              comment += '❌ Failed to extract benchmark results.\n\n';
              comment += `Error: ${e.message}\n`;
            }
            
            // Post comment
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

      - name: Check benchmark performance threshold
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          cd _example
          
          if [ -f benchmark_results.txt ]; then
            # Extract Go Gateway average
            GO_AVG=$(grep "Go Gateway.*avg:" benchmark_results.txt | grep -oP '\d+\.\d+' | head -1)
            APOLLO_AVG=$(grep "Apollo Router.*avg:" benchmark_results.txt | grep -oP '\d+\.\d+' | head -1)
            
            echo "📊 Performance Metrics:"
            echo "  Go Gateway:     ${GO_AVG:-N/A} req/s"
            echo "  Apollo Router:  ${APOLLO_AVG:-N/A} req/s"
            
            if [ ! -z "$GO_AVG" ] && [ ! -z "$APOLLO_AVG" ]; then
              # Simple comparison (we expect Go Gateway to be faster)
              RATIO=$(echo "scale=2; $GO_AVG / $APOLLO_AVG" | bc)
              echo "  Performance ratio: ${RATIO}x"
              
              if (( $(echo "$RATIO >= 1.0" | bc -l) )); then
                echo "✅ Performance benchmark PASSED (Go Gateway >= Apollo Router)"
              else
                echo "⚠️  Performance benchmark WARNING (Go Gateway slower than Apollo Router)"
              fi
            fi
          fi
          
          echo ""
          echo "✅ Benchmark tests completed for main branch"

      - name: Cleanup
        if: always()
        run: |
          docker ps -a -q | xargs -r docker stop || true
          docker ps -a -q | xargs -r docker rm || true
          pkill -f gateway || true
