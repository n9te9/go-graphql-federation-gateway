endpoint: /graphql
port: 9000
service_name: multikey-gateway
timeout_duration: "5s"
request_timeout: "30s"
services:
- name: users
  host: http://users:8080/query
  retry:
    attempts: 3
    timeout: "5s"
- name: badges
  host: http://badges:8080/query
  retry:
    attempts: 3
    timeout: "5s"
