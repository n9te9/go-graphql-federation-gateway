endpoint: /graphql
port: 9000
service_name: fintech-gateway
timeout_duration: "5s"
request_timeout: "30s"
services:
- name: customers
  host: http://customers:8080/query
  retry:
    attempts: 3
    timeout: "5s"
- name: accounts
  host: http://accounts:8080/query
  retry:
    attempts: 3
    timeout: "5s"
- name: transactions
  host: http://transactions:8080/query
  retry:
    attempts: 3
    timeout: "5s"
- name: audit
  host: http://audit:8080/query
  retry:
    attempts: 3
    timeout: "5s"
- name: customers-eu
  host: http://customers-eu:8080/query
  retry:
    attempts: 3
    timeout: "5s"
- name: activities
  host: http://activities:8080/query
  retry:
    attempts: 3
    timeout: "5s"
