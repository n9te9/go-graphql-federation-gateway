endpoint: /graphql
port: 9000
service_name: saas-gateway
timeout_duration: "5s"
request_timeout: "30s"
services:
- name: organizations
  host: http://localhost:8501/query
  retry:
    attempts: 3
    timeout: "5s"
- name: projects
  host: http://localhost:8502/query
  retry:
    attempts: 3
    timeout: "5s"
- name: billing
  host: http://localhost:8503/query
  retry:
    attempts: 3
    timeout: "5s"
