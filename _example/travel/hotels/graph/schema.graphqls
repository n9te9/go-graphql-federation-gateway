extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0",
        import: ["@key"])

type Hotel @key(fields: "id") {
  id: ID!
  name: String!
  location: String!
  price: Float!
  availability: Boolean!
}

type Query {
  hotel(id: ID!): Hotel
}
