services:
  users:
    build: { context: ./users, dockerfile: Dockerfile }
    ports: ["8301:8080"]
  posts:
    build: { context: ./posts, dockerfile: Dockerfile }
    ports: ["8302:8080"]
  comments:
    build: { context: ./comments, dockerfile: Dockerfile }
    ports: ["8303:8080"]
  feed:
    build:
      context: ./feed
      dockerfile: Dockerfile
    ports:
      - "8304:8080"
    environment:
      - PORT=8080
  moderation:
    build:
      context: ./moderation
      dockerfile: Dockerfile
    ports:
      - "8305:8080"
    environment:
      - PORT=8080
  notifications:
    build:
      context: ./notifications
      dockerfile: Dockerfile
    ports:
      - "8306:8080"
    environment:
      - PORT=8080
