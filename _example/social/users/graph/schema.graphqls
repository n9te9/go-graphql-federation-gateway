extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0",
        import: ["@key", "@inaccessible", "@tag"])

type User @key(fields: "id") {
  id: ID!
  name: String! @tag(name: "public")
  email: String! @inaccessible
}

type Query {
  user(id: ID!): User
}
